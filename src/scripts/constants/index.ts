enum ActinTypes {
  PUSH_ROUTER = 'PUSH_ROUTER',
  FETCH_FOO = 'FETCH_FOO',
  RECEIVE_FOO = 'RECEIVE_FOO'
}

export default ActinTypes;

export enum CommonActionType {
  G_SHOW_LOADING = 'G_SHOW_LOADING',
  G_HIDE_LOADING = 'G_HIDE_LOADING'
}
